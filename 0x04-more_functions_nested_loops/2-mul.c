#include "main.h"
/**
  * mul - multiply twi integers
  *@a: -one of the integers
  *@b: - another integer
  *Return: Returns the product of the to numbers
  */
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
